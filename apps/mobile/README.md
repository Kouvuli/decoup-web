# Mobile lives in a separate repository

Mobile lives in `../../../decoup-mb` relative to this directory. That sibling repository contains a React Native + Expo + TypeScript scaffold, private domain placeholders and its own engineering/planning skills. This directory is a historical pointer, not an active workspace. Reuse portable contracts/client helpers through reviewed package contracts when useful; do not import sibling source or share web routes/DOM UI with native screens.
